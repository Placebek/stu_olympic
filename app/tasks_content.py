"""
Тексты заданий по вариантам в формате Markdown.
Каждый вариант — та же задача, но с разными примерами/входными данными.
"""

TASKS: dict[int, str] = {
    1: """\
# Вариант 1 — Maximum Subarray With Equal Products

## Задание

Дан массив положительных целых чисел `nums`.  
Подмассив `arr` называется **product-equivalent**, если выполняется условие:

$$\\text{prod}(arr) = \\gcd(arr) \\times \\text{lcm}(arr)$$

где:
- `prod(arr)` — произведение всех элементов подмассива
- `gcd(arr)` — наибольший общий делитель всех элементов
- `lcm(arr)` — наименьшее общее кратное всех элементов

Подмассив — это непустая последовательность элементов массива, расположенных подряд.  
**Необходимо найти длину самого длинного подмассива, который удовлетворяет этому условию.**

---

## Примеры

### Пример 1
```
Input:  nums = [1, 2, 1, 2, 1, 1, 1]
Output: 5
```
**Объяснение:**  
Самый длинный подходящий подмассив: `[1, 2, 1, 1, 1]`  
- `prod = 2`
- `gcd  = 1`
- `lcm  = 2`
- `prod = gcd × lcm` → `2 = 1 × 2` ✓

### Пример 2
```
Input:  nums = [2, 3, 4, 5, 6]
Output: 3
```
**Объяснение:**  
Подходит подмассив: `[3, 4, 5]`  
- `prod = 60`
- `gcd  = 1`
- `lcm  = 60`
- `prod = gcd × lcm` → `60 = 1 × 60` ✓

---

## Ваш вариант

```
Input:  nums = [1, 1, 2, 1, 1]
```

> Найдите длину самого длинного product-equivalent подмассива.
""",

    2: """\
# Вариант 2 — Maximum Subarray With Equal Products

## Задание

Дан массив положительных целых чисел `nums`.  
Подмассив `arr` называется **product-equivalent**, если выполняется условие:

$$\\text{prod}(arr) = \\gcd(arr) \\times \\text{lcm}(arr)$$

где:
- `prod(arr)` — произведение всех элементов подмассива
- `gcd(arr)` — наибольший общий делитель всех элементов
- `lcm(arr)` — наименьшее общее кратное всех элементов

Подмассив — это непустая последовательность элементов массива, расположенных подряд.  
**Необходимо найти длину самого длинного подмассива, который удовлетворяет этому условию.**

---

## Примеры

### Пример 1
```
Input:  nums = [1, 2, 1, 2, 1, 1, 1]
Output: 5
```
**Объяснение:**  
Самый длинный подходящий подмассив: `[1, 2, 1, 1, 1]`  
- `prod = 2`, `gcd = 1`, `lcm = 2`
- `2 = 1 × 2` ✓

### Пример 2
```
Input:  nums = [2, 3, 4, 5, 6]
Output: 3
```
**Объяснение:**  
Подходит подмассив: `[3, 4, 5]`  
- `prod = 60`, `gcd = 1`, `lcm = 60`
- `60 = 1 × 60` ✓

---

## Ваш вариант

```
Input:  nums = [3, 1, 2, 1, 3, 1]
```

> Найдите длину самого длинного product-equivalent подмассива.
""",

    3: """\
# Вариант 3 — Maximum Subarray With Equal Products

## Задание

Дан массив положительных целых чисел `nums`.  
Подмассив `arr` называется **product-equivalent**, если выполняется условие:

$$\\text{prod}(arr) = \\gcd(arr) \\times \\text{lcm}(arr)$$

где:
- `prod(arr)` — произведение всех элементов подмассива
- `gcd(arr)` — наибольший общий делитель всех элементов
- `lcm(arr)` — наименьшее общее кратное всех элементов

Подмассив — это непустая последовательность элементов массива, расположенных подряд.  
**Необходимо найти длину самого длинного подмассива, который удовлетворяет этому условию.**

---

## Примеры

### Пример 1
```
Input:  nums = [1, 2, 1, 2, 1, 1, 1]
Output: 5
```
**Объяснение:**  
Самый длинный подходящий подмассив: `[1, 2, 1, 1, 1]`  
- `prod = 2`, `gcd = 1`, `lcm = 2`
- `2 = 1 × 2` ✓

### Пример 2
```
Input:  nums = [2, 3, 4, 5, 6]
Output: 3
```
**Объяснение:**  
Подходит подмассив: `[3, 4, 5]`  
- `prod = 60`, `gcd = 1`, `lcm = 60`
- `60 = 1 × 60` ✓

---

## Ваш вариант

```
Input:  nums = [1, 4, 2, 1, 4, 1]
```

> Найдите длину самого длинного product-equivalent подмассива.
""",

    4: """\
# Вариант 4 — Maximum Subarray With Equal Products

## Задание

Дан массив положительных целых чисел `nums`.  
Подмассив `arr` называется **product-equivalent**, если выполняется условие:

$$\\text{prod}(arr) = \\gcd(arr) \\times \\text{lcm}(arr)$$

где:
- `prod(arr)` — произведение всех элементов подмассива
- `gcd(arr)` — наибольший общий делитель всех элементов
- `lcm(arr)` — наименьшее общее кратное всех элементов

Подмассив — это непустая последовательность элементов массива, расположенных подряд.  
**Необходимо найти длину самого длинного подмассива, который удовлетворяет этому условию.**

---

## Примеры

### Пример 1
```
Input:  nums = [1, 2, 1, 2, 1, 1, 1]
Output: 5
```
**Объяснение:**  
Самый длинный подходящий подмассив: `[1, 2, 1, 1, 1]`  
- `prod = 2`, `gcd = 1`, `lcm = 2`
- `2 = 1 × 2` ✓

### Пример 2
```
Input:  nums = [2, 3, 4, 5, 6]
Output: 3
```
**Объяснение:**  
Подходит подмассив: `[3, 4, 5]`  
- `prod = 60`, `gcd = 1`, `lcm = 60`
- `60 = 1 × 60` ✓

---

## Ваш вариант

```
Input:  nums = [2, 1, 3, 1, 2, 1, 1]
```

> Найдите длину самого длинного product-equivalent подмассива.
""",

    5: """\
# Вариант 5 — Maximum Subarray With Equal Products

## Задание

Дан массив положительных целых чисел `nums`.  
Подмассив `arr` называется **product-equivalent**, если выполняется условие:

$$\\text{prod}(arr) = \\gcd(arr) \\times \\text{lcm}(arr)$$

где:
- `prod(arr)` — произведение всех элементов подмассива
- `gcd(arr)` — наибольший общий делитель всех элементов
- `lcm(arr)` — наименьшее общее кратное всех элементов

Подмассив — это непустая последовательность элементов массива, расположенных подряд.  
**Необходимо найти длину самого длинного подмассива, который удовлетворяет этому условию.**

---

## Примеры

### Пример 1
```
Input:  nums = [1, 2, 1, 2, 1, 1, 1]
Output: 5
```
**Объяснение:**  
Самый длинный подходящий подмассив: `[1, 2, 1, 1, 1]`  
- `prod = 2`, `gcd = 1`, `lcm = 2`
- `2 = 1 × 2` ✓

### Пример 2
```
Input:  nums = [2, 3, 4, 5, 6]
Output: 3
```
**Объяснение:**  
Подходит подмассив: `[3, 4, 5]`  
- `prod = 60`, `gcd = 1`, `lcm = 60`
- `60 = 1 × 60` ✓

---

## Ваш вариант

```
Input:  nums = [1, 2, 3, 1, 1, 2]
```

> Найдите длину самого длинного product-equivalent подмассива.
""",
}
